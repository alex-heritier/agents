{
  "name": "agents-cli",
  "version": "1.0.0",
  "description": "A CLI tool to sync AI agent guideline files and slash commands across project hierarchies",
  "type": "module",
  "bin": {
    "agents": "./dist/main.js"
  },
  "scripts": {
    "build": "bun build src/main.ts --outdir dist --target bun --format esm --minify && chmod +x dist/main.js",
    "dev": "bun run src/main.ts",
    "start": "bun run dist/main.js",
    "clean": "rm -rf dist"
  },
  "keywords": [
    "ai",
    "agents",
    "claude",
    "cursor",
    "copilot",
    "cli",
    "guidelines"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "bun-types": "latest"
  },
  "dependencies": {
    "yaml": "^2.3.4"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
